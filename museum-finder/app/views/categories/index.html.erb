<div>

  <h1>Museum Categories</h1>

  <div class="row">
    <div class="large-12 columns"
      <ul class="large-block-grid-3">
      <% @categories.each do |category| %>
        <li><%= link_to category.name, category_path(category)  %></li>
      <% end %>
      </ul>
    </div>
  </div>

<div>